# Preparation workspace

- `intermediate/`: generated normalized episode data
- `reports/`: generated validation summaries

The source of truth is under `sources/`; generated files can be rebuilt with `npm run prepare:data`.
